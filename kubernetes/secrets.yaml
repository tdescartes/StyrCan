apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: styrcan
type: Opaque
stringData:
  # PostgreSQL Credentials
  POSTGRES_USER: "styrcan"
  POSTGRES_PASSWORD: "change-this-password-in-production"
  POSTGRES_DB: "styrcan_db"

  # Database URL
  DATABASE_URL: "postgresql://styrcan:change-this-password-in-production@postgres-service:5432/styrcan_db"

  # MongoDB Credentials
  MONGODB_USERNAME: "styrcan"
  MONGODB_PASSWORD: "change-this-mongodb-password-in-production"
  MONGODB_URL: "mongodb://styrcan:change-this-mongodb-password-in-production@mongodb-service:27017/styrcan_logs?authSource=admin"

  # JWT Secret Key (CHANGE THIS!)
  SECRET_KEY: "your-super-secret-key-change-this-in-production-min-32-chars"

  # Redis Password (optional)
  REDIS_PASSWORD: ""

  # SMTP Credentials (optional - for future use)
  SMTP_USERNAME: ""
  SMTP_PASSWORD: ""
  SMTP_FROM_EMAIL: "noreply@styrcan.com"
